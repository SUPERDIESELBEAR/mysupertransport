

## Update Sample Phone Number in Forms Catalog

### Change
In `src/components/management/FormsCatalog.tsx`, update the sample phone number from `816-555-0100` to `(816) 555-0100` in the `SAMPLE_DATA` object.

### File
| File | Change |
|------|--------|
| `src/components/management/FormsCatalog.tsx` | Update `phone` value in `SAMPLE_DATA` |

