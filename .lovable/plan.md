Remove all `placeholder` example text from the inputs in `src/components/pei/AddPreviousEmployerModal.tsx` so fields render fully blank:

- Employer name: remove "ABC Trucking, Inc."
- Contact name: remove "Jane Doe"
- Contact email: remove "hr@company.com (can be added later)"
- City: remove "Dallas"
- Start (MM/YYYY): remove "06/2020"
- End (MM/YYYY): remove "09/2023 (blank = present)"

No other logic, labels, or validation changes.