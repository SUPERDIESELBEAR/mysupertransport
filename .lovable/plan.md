
This is a research question, not an implementation task. Let me check the ICA workflow before answering.

Based on the memory file `mem://features/ica-management/unified-workflow` and the codebase structure (`src/components/ica/ICABuilderModal.tsx`, `ICADocumentView.tsx`, `ICAViewModal.tsx`, `src/components/operator/OperatorICASign.tsx`), the existing ICA workflow handles operator signatures (canvas-based signing on mobile) but I should verify whether a company/owner countersignature feature exists.

Let me investigate the ICA components to give an accurate answer.
