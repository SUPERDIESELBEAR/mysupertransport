# Fuel report mismatch — findings

Nothing was changed. This is a report.

## 1. The pages you saw were FIXTURE, not database

They were made from an invented sample: 34 identical made-up purchases named
"Pilot Travel Center #100"–"#1033", all Oklahoma City, all $625.26. One of them
was hand-marked as deducted purely so the settled half of the layout had
something in it to check.

How that was established:

- The sample was generated by a throwaway file outside the project that builds
  its rows in code (`Array.from({length: 34}, ...)`) and never opens the
  database. That is why 34 rows are byte-identical — real purchases never are.
- The database says otherwise. Ali has exactly 3 fuel rows, $1,960.56, with
  three different merchants in three states, and no duplicates anywhere in the
  file: the largest number of purchases held by any driver is 5, and the whole
  import is still 69 rows, $31,913.66.
- `settlement_line_items` has zero rows sourcing fuel, so no settled label can
  come from live data — which is itself proof the settled row was invented.

So case 3 does not apply: nothing is duplicating rows and the dedupe key did not
fail.

## 2. What was and was not verified

The layout work stands, and the three page findings are real: the columns did
overflow the page, cells were being cut, and page two carried no name.

The report's claim that the pages matched both screens **for a real driver was
not verified that way**. Figures for Ali were checked against the database in a
separate step, but the pages that were rendered and reviewed were the invented
sample. Saying "matched both screens" while showing sample pages was wrong, and
the wording implied a check that had not been run in that form.

## 3. Ali's report, generated now from the live rows

| Date | Merchant | Location | Fuel | Cash advance | Discount | Total | Gallons | $/gal | Deducted |
|---|---|---|---|---|---|---|---|---|---|
| 09/01/2026 | Flying J #733 | Lubbock, TX | $514.08 | — | -$8.12 | $505.96 | 81.23 | $5.699 | Not yet |
| 09/01/2026 | Pilot Travel Center #1033 | Midland, TX | $122.30 | $505.00 | -$2.04 | $625.26 | 20.39 | $5.998 | Not yet |
| 08/29/2026 | Loves #822 | Clarksville, AR | $829.34 | — | — | $829.34 | 132.06 | $5.889 | Not yet |

Taken out of settlements: **$0.00, 0 purchases.**
Not yet deducted: **$1,960.56, 3 purchases**, fuel $1,465.72, cash advance
$505.00, discount -$10.16, gallons 233.68.
Filename `fuel-ali-mohamed-2026-08-29-to-2026-09-01.pdf`. No settled row appears.

## 4. Do the three agree today

The report and the office screen: yes — same three rows, same totals, produced
from the live rows above by the same builder both screens use.

The driver's own screen: **not proven today, and it cannot be proven from here.**
His screen reads through a door that identifies him from his signed-in session,
and this sandbox cannot sign in as him. What is established is that his door
selects the same columns off the same table and joins settlements the same way,
so the inputs are the same; but "the numbers match today" for his screen is
unverified, and I am not going to claim it.

One further live detail: his unit number is empty in the system, so the report
header reads "Ali Mohamed" with no unit. The "Unit 260" in the sample was
supplied by hand.

## Technical notes

- Live check: `fuel_transactions` grouped by operator — max 5 rows for any
  driver; Ali's operator `dbe31d0d…` has 3 rows totalling 1960.56; invoice
  numbers 5533430, 1333199, 50895.
- `settlement_line_items where source_table='fuel_transactions'` → 0 rows.
- `my_fuel_transactions()` joins `operators o on o.user_id = auth.uid()`; with no
  session it returns nothing, which is why the operator path cannot be compared
  by direct query here.
- The live report above was produced by feeding the queried rows through
  `buildDriverRows` + `buildFuelPdfDocument` in a scratch script outside the
  project. No project file was modified.

## What I suggest next

Correct the 2026-09-09 record so it states the pages reviewed were a sample, the
real-driver parity claim was not verified as written, and the live figures above
are the verified ones. Separately, decide whether proving the driver-side figures
needs a signed-in check rather than a structural argument.
