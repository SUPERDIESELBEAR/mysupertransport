# Bring the driver calendars back

Nothing was deleted. Every dispatched / home / truck down / not dispatched day is still stored — the calendars are showing a question mark and no colours because the new Absence Log asks for a reason field that only exists after this draft is accepted. The request comes back as an error, so the calendar receives no days at all and falls back to "unknown".

Two ways forward:

- **Revert** — you can undo my changes yourself with the revert arrow on the message before the Absence Log work, or by picking that version in the History tab at the top of the chat.
- **Fix forward (this plan)** — make the calendar and the Absence Log work whether or not the new field is there yet, so the colours and history come back straight away and the reason-per-day feature switches on the moment you accept the draft.
