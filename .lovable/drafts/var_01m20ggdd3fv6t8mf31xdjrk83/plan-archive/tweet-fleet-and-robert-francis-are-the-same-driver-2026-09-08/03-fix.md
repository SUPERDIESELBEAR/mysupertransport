## What to do

**1. Correct this driver's account name**
Set the account record for unit 263 to **Robert Francis**, matching his application. One record changes; nothing else is touched.

**2. Make all screens agree**
Driver Status currently shows only the account-record name. Give it the same rule Driver Hub uses: prefer the application name, fall back to the account record when there is no application. Then a mismatched account name can never hide a driver from a screen again.

**3. Stop it happening again**
A driver renaming themselves in their own app currently leaves no trail. Record a proper entry every time a name is changed that way — who, when, from what to what — so it shows up in the activity log exactly like a staff edit does. The driver keeps the ability to fix a typo; you just get to see it.

## Option 4 — the rest of the roster, checked live

Every active and archived driver was compared. Only **two** real mismatches exist:

| Unit | Application says | Account says | Note |
|---|---|---|---|
| 263 | Robert Francis | TWEET FLEET | fixing in step 1 |
| 249 | Trovino Huddleston | Vino Huddleston | short form of the same name — your call |

The other ten differences are only stray extra spaces in the application name (e.g. "Deneric  Guidry ") — same name, no display effect. The fallback rule in step 2 trims those, so they need no data change.

## Two decisions

- Unit 249: leave "Vino" as-is, or match it to "Trovino"?
- Should the two names be shown together anywhere (e.g. "Robert Francis" with a quiet note that he goes by Tweet Fleet), or is the application name the only one you want on screen?
