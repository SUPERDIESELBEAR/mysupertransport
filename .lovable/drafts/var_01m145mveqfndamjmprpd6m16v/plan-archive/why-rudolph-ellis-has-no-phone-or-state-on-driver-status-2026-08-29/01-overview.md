# Why Rudolph Ellis has no phone or state on Driver Status

Rudolph's data, checked live:

- Application: phone `(337) 886-8585`, state `LA` — complete.
- Account record (his login profile): phone empty, home state empty.

Driver Status reads only the account record, so his row comes out blank. The Contact Info card on his driver page and the Driver Hub roster read the application, which is why they look fine.

**The account record is not a form anyone fills in.** It is created automatically when the driver gets a login, and it stores their name, avatar, phone, and home state for use across messaging, dispatch, and notifications. Nothing copies the phone and state from the application into it, so unless a staff member happened to edit contact details by hand, those two fields stay empty. Of 61 active drivers, 34 are in exactly this state. No one omitted anything.
