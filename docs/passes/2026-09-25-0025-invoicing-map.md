Prompt received complete, ending with the line "END OF PROMPT — the first line of your report must say whether you received this prompt complete, ending with this line."

# Alvys milestone 2, pass 0 — invoicing map (2026-09-25 00:25 UTC)

Read-only. No code, data, migration or function changed. No real row written. Invoice ST26-0001 was read only. Full suite skipped (docs only).

## Exact counts (live, count(*))

| Table | Rows |
| --- | --- |
| invoices | 1 |
| invoice_line_items | 1 |
| invoice_batches | 0 |
| invoice_number_config | 1 (2026 / ST / "-" / padding 4 / next_sequence 2) |
| load_charges | 4 |
| payments | 0 |
| factoring_remittances | 0 |
| ar_aging_snapshots | 0 |
| brokers | 13 |
| broker_factoring_history | 0 |
| load_documents | 25 |
| document_send_log | 0 |
| document_exceptions | 0 |
| accessorial_adjustments | 2 |
| loads | 18 |
| settlements | 1 |
| rm_deposits | 0 |

**The one invoice:** ST26-0001, load ST-TEST-005, Test Broker Beta, direct, $1,875.00, status `open`, never submitted/purchased/paid/reconciled, billing email snapshot empty, one `linehaul` line $1,875.00, created 2026-09-04 by Marcus Mueller, `updated_at` 2026-09-17 21:37 (the earlier probe incident; values now match the Pass 3 record).

**The 18 loads** (status · type · broker factoring status · documents):

| Load | Status | Type | Broker (factoring) | Docs |
| --- | --- | --- | --- | --- |
| ST-TEST-001 | in_transit | standard | Test Broker Alpha (approved) | 4: pod, rate con, revised rate con |
| ST-TEST-002 | in_transit | standard | Test Broker Alpha (approved) | 0 |
| ST-TEST-003 | delivered | per_ton | Test Broker Beta (not_approved) | 2: pod, scale ticket |
| ST-TEST-004 | dispatched | loadout | none | 0 |
| ST-TEST-005 | invoiced | standard | Test Broker Beta (not_approved) | 0 |
| ST26003 | available | standard | Test Broker Alpha (approved) | 0 |
| ST26015 | available | standard | Cahaba Transportation (unknown) | 1: rate con |
| ST26033 | cancelled | standard | Blue Grace (unknown) | 2 |
| ST26034 | cancelled | standard | Blue Grace (unknown) | 5 |
| ST26035 | covered | standard | Blue Grace (unknown) | 3 |
| ST26056 | delivered | standard | Globaltranz (unknown) | 1: rate con |
| ST26058 | delivered | standard | ITS National (unknown) | 1: rate con |
| ST26059 | delivered | standard | Eclipse Transervices (unknown) | 1: rate con |
| ST26060 | delivered | loadout | Rolling River (unknown) | 1: rate con |
| ST26061 | tonu | standard | Fide Freight (unknown) | 1: rate con |
| ST26063 | delivered | standard | Nationwide (unknown) | 1: rate con |
| ST26064 | available | standard | US Logistics (unknown) | 1: rate con |
| ST26069 | available | standard | Openroad Global (unknown) | 1: rate con |

Zero loads at `pod_received`, `accessorials_approved`, `ready_to_invoice`, `factored`, `paid`, `settled`, `closed`.

## STEP 1 — the flow as it is today

1. **Ready to invoice — EXISTS, PROVEN ONCE (test load only).** Statuses delivered → pod_received → accessorials_approved → ready_to_invoice, moved by staff through `update_load_status` (the status buttons); nothing moves them automatically (the 0065 rule stops at `delivered`). Paperwork: `loadPaperwork.ts` marks POD required (plus scale ticket on per-ton, loadout photo slots on loadouts) and BOL expected — but this is shown on screen only; `update_load_status` does not refuse `ready_to_invoice` when the POD is missing. Evidence: ST-TEST-005 reached it with 0 documents. Billing statuses (invoiced/factored/paid/settled) are management/owner only.
2. **Invoice creation — EXISTS AND PROVEN (1 invoice).** `create_invoice(uuid, jsonb)`, SECURITY DEFINER, management or owner only (P22 says dispatcher too — not yet). Client builds with `invoiceBuilder.ts`; the function refuses mismatched totals, a load not at `ready_to_invoice`, a second invoice, and a charge set that differs in either direction; billing path read from the broker (only `approved` = factored). Number from `allocate_invoice_number` on the write, per carrier (`invoice_number_config` keyed company+year) → `ST26-0001`. Contains linehaul, unbundled FSC, every load charge, and late adjustments at full amount; the one real invoice has a linehaul line only. Moves the load to `invoiced`. Screen: Management → Accounting → Billing queue (`BillingQueuePage.tsx`).
3. **Invoice document — MISSING.** No PDF is produced anywhere; no invoice file in any storage bucket; no screenshot exists. Nearest: `broker_billing_email_snapshot` column (empty on the one invoice).
4. **Load documents — EXISTS AND USED.** 13 types: rate_confirmation, revised_rate_confirmation, bol, pod, scale_ticket, lumper_receipt, detention_documentation, loadout_pickup_inspection, loadout_delivery_inspection, permit, broker_correspondence, other, reimbursement_proof. Files in private bucket `load-documents`; 25 rows live. Joining PDFs: `pdf-lib` ^1.17.1 is already a dependency (browser) and is Deno-importable for functions; `jspdf` ^4.2.1 and `pdfjs-dist` also present. `generate-application-pdf` is an existing edge-function PDF pattern. Nothing joins PDFs today; image files (photos) would need converting to pages.
5. **Delivery — MISSING.** Nothing sends an invoice by email, portal or API. `document_send_log` has 0 rows and no invoice writer. No Smart Freight Funding submission exists.
6. **Factoring — PARTLY EXISTS.** Broker: `brokers.factoring_status` (approved/not_approved/unknown/pending) + reason + updated_at, history via trigger into `broker_factoring_history` (0 rows); 3 approved (both test brokers "Alpha" count as one broker), 9 of 13 `unknown`, 1 `not_approved`. Carrier's factor: **MISSING** — `carrier_profile` has no factor name, submission address or account; the only factoring % is `dispatch_settlement_rates.factoring_pct` (the dispatch-company settlement). `record_factoring_remittance` defaults source to `'smart_freight_funding'` as text. "Factored": load status `factored` exists but nothing sets it; invoice has `submitted_at`/`purchased_at` (purchase only allowed on factored path) and no writer sets either; `invoice_batches` (0 rows) has no writer. "Factoring payment received" would need: the factor submission (submitted_at + batch), the purchase/advance (purchased_at, load → `factored`), and the remittance posting (below) — and a link to settlement eligibility, which does not exist.
7. **Payments — EXISTS BUT NEVER USED.** `payments` (0) and `factoring_remittances` (0). Writers: `record_invoice_payment` (direct, management/owner), `record_factoring_remittance` (statement header + lines, matches on digits of our invoice number, never guesses, holds unmatched lines), both via `post_invoice_payment_internal` → invoice `partial`/`paid`, and on paid calls `update_load_status(…,'paid')`. `close_short_paid_invoice` closes a partial with a reason. Client helpers in `src/lib/remittance.ts` exist, **no screen calls them**. Settlement eligibility: `store_settlement_run` has zero references to invoices or factoring — **a driver settlement does not wait for the factoring payment today (MISSING)**.
8. **Void / re-issue / corrections — MISSING.** No void action (P23). `enforce_invoice_immutability` freezes load/broker/number/company; `enforce_invoice_line_immutability` freezes lines. `invoices_load_key UNIQUE(load_id)` blocks any re-issue after a void and any supplemental (-A1) invoice row — the open Module 7 Pass 5 decision. `invoice_status` has no `void` value.
9. **Who may do what today.** Create invoice: management, owner (P22 adds dispatcher — MISSING). Void: nobody (P23 = owner, management). Close short pay: management, owner (P33 keeps dispatch out — matches). Payments, remittances, batches, AR snapshots: management, owner (policies + function checks). View invoices: also `invoice.view` permission. Load charges and load documents: dispatcher, management, owner write; drivers insert paperwork on their own load.

## STEP 2 — gaps against Milestone 2

| Item | Exists | Missing | Risk |
| --- | --- | --- | --- |
| Paperwork before invoicing | Required/expected list on screen | Database refusal of `ready_to_invoice` without POD (or an approved exception) | Invoice goes out without POD; factor rejects it |
| Invoice PDF | Lines and totals in the database | PDF generation, storage, fields (bill-to address, remit-to, terms, references) | Nothing to send |
| Packet (P53) | Documents in `load-documents`; pdf-lib available | Per factor/customer setting (separate vs stitched), assembly, image→page | Wrong packet shape rejected by factor |
| Carrier's factor record | Only a text default and a dispatch-company % | Factor name, submission email/portal, NOA/remit-to, advance/fee % per carrier | Hard-coded Smart Freight identity (SaaS rule) |
| Factor submission | `submitted_at`, `invoice_batches` columns | A writer, a send, a log | No record of what was sent |
| Factoring payment received | Remittance posting functions | Screen; purchase/advance step; load → `factored` | Money received but unrecorded |
| Settlement eligibility | Nothing | Settlement engine reading "factoring payment received" | Drivers paid before the carrier is funded (or never, if wired wrong) |
| Direct-bill posting | `record_invoice_payment`, short-pay close | Screen; broker billing email is empty on invoices | Unapplied cash, no AR |
| Void / re-issue | Nothing | P23 action, `void` status, re-issue path | Wrong invoice cannot be corrected |
| Supplemental -A1 | Adjustments approved and settled | Container (table vs relaxed key) | Late accessorials never billed to broker |
| Dispatcher issuing (P22) | — | Function + three policies widened together | Dispatch cannot invoice |
| Company filter in definer functions | Policies filter by company | See Step 4 | Second carrier could touch the first carrier's invoices |

## STEP 3 — proposed passes, in order

1. **Tenancy fixes for billing functions** (Step 4 items). Proves: a scratch second carrier's management is refused on a SUPERTRANSPORT load/invoice/remittance line; residue 0.
2. **Paperwork gate for ready_to_invoice.** Proves: refusal without POD (scale ticket on per-ton, photos on loadout), pass with an approved document exception. **OWNER DECIDES:** which documents must be present before a load can be invoiced (POD only / POD + BOL / POD + lumper receipt when a lumper is charged); and whether a load may move to `ready_to_invoice` by itself when paperwork and accessorials are done, or stay a staff button.
3. **Carrier factor record + dispatcher issuing (P22).** Per-carrier factor settings (name, submission address, remit-to) and dispatcher allowed to create invoices. Proves: dispatcher can create, cannot void/close; no carrier identity in code. **OWNER DECIDES:** Smart Freight Funding's submission method (email address, portal upload, or both) and the exact remit-to/Notice of Assignment wording on invoices.
4. **Invoice PDF.** Generated from stored lines, kept in storage, shown on the load page. Proves: the PDF total equals the database total to the cent on the test invoice (in a rolled-back or scratch setting — ST26-0001 untouched). **OWNER DECIDES:** payment terms printed (e.g. Net 30), and whether invoice numbers should show as `ST26-0001` or match the old Alvys digit style.
5. **Packet (P53).** Separate PDFs or one stitched PDF, chosen per factor/customer. Proves: page count and order; photos become pages. **OWNER DECIDES:** document order in the packet (invoice, rate con, BOL, POD, lumper…), and whether the rate confirmation is included.
6. **Submission + send log.** Send to the factor (or direct-bill broker), set `submitted_at`, write `document_send_log`/batch. Proves: send logged once; resend logged separately. **OWNER DECIDES:** one email per invoice or one batch per day to the factor.
7. **Factoring payment received + remittance screen.** Screen over `record_factoring_remittance`; purchase step sets `purchased_at` and load → `factored`. **OWNER DECIDES:** what "factoring payment received" means for settlement — the day the factor advances (purchase), or the day the remittance statement posts; and how factor reserves/chargebacks are handled if Smart Freight ever holds any (the one real statement showed none).
8. **Settlement eligibility.** Settlement run only includes loads whose factoring payment is received (or direct payment received). Proves against a scratch week. **OWNER DECIDES:** what happens to a direct-bill load when the broker has not paid — driver waits, or paid on delivery like today.
9. **Direct-bill posting screen.** Over `record_invoice_payment` and `close_short_paid_invoice`. Proves: partial → paid; short-pay close needs reason.
10. **Void and re-issue (P23) + supplemental -A1 (Module 7 Pass 5).** **OWNER DECIDES:** (a) after a void, the new invoice gets a new number, or the same number with a suffix (ST26-0001-R); (b) a late accessorial is billed as a separate supplemental invoice (ST26-0001-A1) or by voiding and re-issuing the whole invoice. Option (a)/(b) decides whether `invoices_load_key` is relaxed or a `supplemental_invoices` table is added.

## STEP 4 — tenancy check (report only, nothing fixed)

SECURITY DEFINER functions reading or writing invoice, payment or factoring data. RLS does not apply inside them, so the body must filter by company.

| Function | Callable by | Filters by company in body? | Finding |
| --- | --- | --- | --- |
| create_invoice | authenticated | **No** | Reads the load and broker by id with no company check; another carrier's management could invoice a SUPERTRANSPORT load if given its id. Stamping writes the caller's company onto it. |
| record_invoice_payment | authenticated | **No** | Posts a payment to any invoice id, any carrier. |
| post_invoice_payment_internal | nobody (internal) | No | Relies on callers; reached by the two above. |
| record_factoring_remittance | authenticated | **Partly** | Duplicate check filters by company; the invoice MATCH does not — a second carrier's statement line with digits `260001` would match SUPERTRANSPORT's ST26-0001. |
| close_short_paid_invoice | authenticated | **No** | Any invoice id, any carrier. |
| create_accessorial_adjustment | authenticated | Yes (reads load's company for rates) | Reads invoices for the -A1 reference; role check present; the caller's company is not compared with the load's company — worth confirming in pass 1. |
| allocate_invoice_number | nobody (service_role) | Yes | OK. |
| create_carrier | nobody (platform) | Yes | Seeds numbering; OK. |
| enforce_invoice_immutability, enforce_invoice_line_immutability, enforce_ar_aging_snapshot_append_only | trigger only | n/a | Guards; no client EXECUTE. |
| log_broker_factoring_change, stamp_broker_factoring_status_change | trigger only | n/a | Company comes from the row. |
| store_settlement_run | authenticated | No invoice/factoring reads (matched on "payments" wording only) | Out of this area; not reviewed here. |

All pinned to `search_path = public, extensions`; none executable by anon.

## Contradictions

None with the prompt. Noted against the records: P22 (dispatcher may issue) is not live — `create_invoice` and the three billing policies are management/owner only, as the 2026-09-21 inventory already says.

## Files changed (git)

- docs/passes/2026-09-25-0025-invoicing-map.md (new)
- docs/tms-build-status.md
