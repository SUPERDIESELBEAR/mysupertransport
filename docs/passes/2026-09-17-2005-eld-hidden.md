# Pass report — 2026-09-17 2005 UTC — ELD/RODS hidden, grant-parity harness grant, two record corrections

Immutable. Do not edit after commit.

## Scope

Owner decision (b), 2026-09-17: HIDE the ELD/RODS duty-status feature from
drivers and staff, KEEP the data, tables, functions, triggers, policies and
buckets, KEEP the inspection binder working, reversible by design. Plus the
grant-parity harness grant and two record corrections. Read first:
`docs/passes/2026-09-17-1730-eld-tidyup-and-removal-inventory.md` and its record
entry (B3, B4, B5(b)).

## STEP 1 — the line, drawn before any change

**HIDE**

- `/roadside` boot branch (`src/main.tsx`), its service-worker offline fallback
  entry (`vite.config.ts`), and the Roadside PWA shortcut (`public/manifest.json`).
- `/eld/officer-email` → `/eld/*` renders the notice (`src/App.tsx`).
- `SyncRunnerMount` no longer mounted (`src/App.tsx`); `startSyncRunner()`
  short-circuits (`src/lib/eld/offline/queue/runner.ts`).
- Operator portal: Paper Logs and ELD Malfunction nav entries, malfunction
  banner, roadside cache hydration, lazy view imports.
- Management portal: Paper Logs (RODS), Retention Archive, ELD Malfunctions,
  Device Models entries and lazy imports.
- Cron jobs 402 `rods-certification-reminders-hourly` and 412
  `process-eld-escalations`.
- **Officer packets: HIDE.** They are the roadside/duty-status staff workflow
  (officer email sheet plus `/roadside`), hold zero rows, and nothing outside the
  feature depends on them. They are not the binder's `/inspect/:token` links.

**KEEP REACHABLE** — inspection binder (driver + management),
`truck_dot_inspections`, `inspection_documents` and versions,
`v_compliance_items` and the compliance alerts panel, MO plate expiry sync and
License Plate Registry, `/inspect/:token` and document short links, binder share
bundles, onboarding's binder writes, quarterly inspection program, cron job 7
`daily-inspection-expiry-check`.

Nothing ambiguous was found; nothing was deleted.

## STEP 2 — the ways in

New: `src/lib/eld/featureVisibility.ts` (`ELD_FEATURE_HIDDEN = true`,
`ELD_HIDDEN_MESSAGE = 'This feature is not available.'`) and
`src/components/eld/EldHiddenNotice.tsx`.

**Chosen behaviour: a plain notice, not a redirect.** A redirect loses the person
who typed the URL and hides the fact that the screen is gone. Every hidden view
value still resolves inside its portal shell and renders the notice; no crash,
no blank screen. `src/lib/eld/`, `src/roadside/` and the ELD components remain.

## STEP 3 — the writing stopped

- `src/App.tsx` — `SyncRunnerMount` import and mount removed.
- `src/lib/eld/offline/queue/runner.ts`, `startSyncRunner()` first statement:
  `if (ELD_FEATURE_HIDDEN) return;` — before `started`, before any `addEventListener`,
  before any drain.
- `public/service-worker.js` is a one-release cleanup worker: no `sync`,
  `periodicsync` or fetch-caching task. `vite.config.ts` has no background sync.
- Source sweep for writers to the ten ELD tables from a driver session: the only
  paths are `src/lib/eld/**` (queued through the runner) and the hidden screens.
  Roadside-**stop** components (`roadside_stops` inspection records) are separate
  and were left alone.

Tables and policies untouched.

## STEP 4 — cron, with a contradiction the owner resolved

The prompt said four jobs. Live `cron.job` had **two** duty-status jobs.

BEFORE:

```text
402 | rods-certification-reminders-hourly | 5 * * * *  | active
412 | process-eld-escalations             | 0 * * * *  | active
  7 | daily-inspection-expiry-check       | 0 15 * * * | active
```

`cron-inspection-reminders` (the fourth name) has source but **no `cron.job` row
at all**. Job 7 sends CDL / medical / IRP / IFTA / insurance / Periodic DOT
Inspection expiry emails — KEEP side. The pass stopped and asked; the owner
directed: stop only the two log jobs.

AFTER (migration `0004`): jobs 402 and 412 absent; remaining job ids
`5,6,7,8,9,10,12,13,15,16,106,455`, job 7 still active. The edge functions both
jobs called were kept.

**Emails that stop:** hourly RODS certification reminders; ELD malfunction
escalations. Nothing else.

## STEP 5 — grant-parity fix

Migration `0004` also runs
`GRANT EXECUTE ON FUNCTION public.grant_parity_report() TO sandbox_exec`.
`psql` connects as `sandbox_exec` (`current_user` and `session_user` both); the
old ACL named `sandbox_exec_qgxpkcudwjmacrdcyvhj`. Catalog sweep of every
`public` function whose name contains `parity` or `report`: `grant_parity_report`
is the only one with the problem.

Before:

```text
ERROR:  permission denied for function grant_parity_report
```

After:

```text
 Test Files  1 passed (1)
      Tests  3 passed (3)
```

**Dark since at least 2026-09-14 23:43 UTC**, not since batch 4's 15:45 run — the
identical error is quoted in
`docs/passes/2026-09-14-2343-federal-breaks-tenancy.md`. Three days.

## STEP 6 — verified by hand, real sessions, one sign-in per identity

**Steve Figueroa (driver).** Nav: Home, Status, Upload Docs, Document Hub,
Binder, DOT, My Docs, My Truck, Resources, Pay Setup, Settlements, Fuel,
Forecast, ICA, Dispatch, Messages, FAQ, Alerts — **no Logs entry**.

```text
=== steve /operator/paper-logs      -> This feature is not available.
=== steve /operator/eld-malfunction -> This feature is not available.
=== steve /roadside                 -> This feature is not available.
=== steve /eld/officer-email        -> This feature is not available.
```

Documents/uploads worked (Form 2290 Received, Truck Title Received, twelve truck
photos, Truck Inspection Report Received); the binder rendered.

**Mae Lauron (management).** SAFETY & COMPLIANCE contains only **DOT Inspection
Binder**; no Paper Logs (RODS), Retention Archive or ELD entries.
`?view=eld-logs`, `?view=eld-malfunctions`, `?view=eld-retention` each render
"This feature is not available." Still working: Fleet Compliance ("Compliance
Alerts 8 … 4 expired · CDL or medical cert expiring within 30 days"), License
Plate Registry, Onboard Systems ("ELD 61 — 11 Available · 43 Assigned", equipment
inventory, not duty status).

**Signed out.** `/inspect/c8119ab9-…`: "SUPERTRANSPORT · Roadside Document
Viewer · Secure Link · CDL (Back) · Valid · Expires: Nov 24, 2028" with
Open/Save.

Observation, not changed: the driver binder still says "Always carry at least 8
days of paper log pages in your vehicle." Binder guidance, not a way in.

## STEP 7 — tests skipped, none deleted

- `src/test/view-reachability.test.ts` — aggregate "every view is reachable" case
  `it.skip`, reason names the 2026-09-17 hide decision (six views deliberately
  unreachable).
- `src/test/navigation-title-invariant.test.ts` — the removed **Device Models**
  placement moved to a documented `hiddenPlacements` array with a scoped
  `it.skip.each`.

No binder or KEEP REACHABLE test was touched. Skipped tests went 15 → 17.

## STEP 8 — record corrections (appended, not rewritten)

(a) The 1730 entry's "offline sync NOT tested" is corrected to **"tested, results
not quoted"** — the stopped turn ran the offline queue/sync cases before and
after `0003` but quoted nothing.
(b) `ee993ec0`: **downgraded from unexplained to probably explained** — the owner
says his team likely deactivated a driver around 13:37 UTC on 2026-09-17, which
matches `updated_at = 2026-09-17 13:37:42`. Nothing records it
(`deactivated_by` NULL, no `audit_log` row), so it stays a gap.

## STEP 9 — record and follow-up list

`docs/tms-build-status.md`: dated entry "2026-09-17 (later) — ELD/RODS HIDDEN
(owner decision (b))…", sections (a)–(g), including the exact hidden list for a
future undo. `docs/tms-wish-list.md`: the keep/hide/remove decision moved to
RECENTLY CLOSED (decided: hide); "ELD/RODS FULL REMOVAL, if the feature is still
unused later — the binder must be unpicked first" added under DECIDED, NOT
BUILT; the grant-parity VERIFICATION GAP removed, closed by Step 5.

## STEP 10 — full suite and typecheck

```text
 Test Files  202 passed | 2 skipped (204)
      Tests  2014 passed | 17 skipped (2031)
     Errors  2 errors
   Start at  19:08:37
   Duration  372.55s
EXIT=1
```

Both errors are the known reporter-level `[vitest-worker]: Timeout calling
"onTaskUpdate"`; they are not attached to a test file, so there is no single file
to re-run — the non-zero exit comes from them alone, with zero failing tests.
The one previously failing test (`grant-parity-live.test.ts`) was re-run alone and
passed 3/3, quoted in Step 5.

Typecheck: `npx tsgo -p tsconfig.app.json --noEmit` → `TSC_EXIT=0`.

## Files changed — `git diff --stat` for the pass (base `1143318e1`)

```text
 docs/tms-build-status.md                           | 134 +++++++++++++++++++++
 docs/tms-wish-list.md                              |   7 +-
 ...e_duty_status_crons_and_grant_parity_report.sql |  27 +++++
 drizzle/migrations/meta/0004_snapshot.json         |  18 +++
 drizzle/migrations/meta/_journal.json              |   7 ++
 public/manifest.json                               |   8 --
 public/version.json                                |   4 +-
 src/App.tsx                                        |  44 +++----
 src/components/eld/EldHiddenNotice.tsx             |  15 +++
 src/lib/eld/featureVisibility.ts                   |  21 ++++
 src/lib/eld/offline/queue/runner.ts                |   6 +
 src/main.tsx                                       |  25 ++--
 src/pages/management/ManagementPortal.tsx          |  54 +++------
 src/pages/operator/OperatorPortal.tsx              |  69 +++--------
 src/test/navigation-title-invariant.test.ts        |  18 ++-
 src/test/view-reachability.test.ts                 |   9 +-
 vite.config.ts                                     |   6 +-
 17 files changed, 339 insertions(+), 133 deletions(-)
```

Plus this report. `src/integrations/supabase/types.ts` was regenerated by the
migration tool and shows no diff.

## Contradictions between the prompt and the live system

1. **"the four cron jobs"** — only two duty-status jobs existed (402, 412); job 7
   is compliance/binder and on the KEEP side; `cron-inspection-reminders` has no
   `cron.job` row. Stopped and reported; the owner directed stopping only the two.
2. **"dark since batch 4's 15:45 UTC run"** — it has been dark since at least
   2026-09-14 23:43 UTC.

Nothing else contradicted the record.
