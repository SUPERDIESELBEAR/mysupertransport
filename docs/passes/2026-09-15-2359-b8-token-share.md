# Pass report — 2026-09-15 23:59 UTC

## Scope

B8 — the token and share tables, plus `preview_sessions` and
`passenger_authorizations`. The last batch of the `company_id` sequence, and the
only one whose writers were believed to be anonymous.

This pass RESUMED after `docs/passes/2026-09-15-2333-b8-stopped-on-contradictions.md`.
Both contradictions in that stop were upheld by the owner and became decisions.

## Decisions applied (owner, this pass)

1. `document_short_links` takes Shape 1; the record calling
   `get_or_create_short_link` an anonymous writer was wrong.
2. `share_token_access_log` stays GLOBAL — a cross-carrier abuse log is not
   tenant data.
3. The third stamping shape covers ZERO tables.
4. `share_tokens` gets `company_id` AND a company-scoped read policy.

## Migration

One migration, seven tables, Shape 1: `share_tokens`, `document_short_links`,
`binder_share_bundles`, `ica_review_links`, `officer_packet_links`,
`preview_sessions`, `passenger_authorizations`. Nullable add → backfill from the
sole `carrier_profile` (guarded: the DO block raises if the row count is not
exactly 1) → NOT NULL, no default, `ON DELETE RESTRICT` FK, `company_id` index,
`aa_stamp_tenant_company_id` trigger. `share_tokens` SELECT and UPDATE policies
re-created with `company_id = public.current_company_id()` ANDed to the existing
role tests. `share_token_access_log` received a table comment recording why it
is global.

Structural check after: all seven show `attnotnull = t`, one stamp trigger, one
RESTRICT FK, one index. Zero null or orphan `company_id` across all seven.

## Client / function adaptations

| File | Company source |
| --- | --- |
| `send-officer-packet` | `companyIdForOperator` (packet's driver) |
| `send-binder-share` | `companyIdForAnyUser`, bundle skipped if unresolved |
| `send-ica-review-link` | `companyIdForUser`, 403 without membership |
| `create-preview-session` | `companyIdForUser`, 403 without membership |
| `send-passenger-auth` | the membership already queried |

## Anonymous path verified end to end

`resolve_share_token` read live: `SECURITY DEFINER`, `SET search_path TO
'public','extensions'`, lookup by token, no `current_company_id`. An
unauthenticated REST call with the publishable key on live token
`c8119ab9-a66c-4a72-b2d9-3c445f5f8bbd` returned the signed document URL, and
`share_token_access_log` gained one `ok` row with no company column. Playwright,
signed out, on `/inspect/<token>`: "Roadside Document Viewer / Secure Link /
CDL (Back) / Valid". Console errors were pre-existing React ref warnings.

## Guards (six new, in `src/test/tenancy-resolver.test.ts`)

Shape-1 structure of the seven; `share_token_access_log` never gains the column
AND its parentless rows stay non-zero; no stamp function derives from a share
token (the third shape stays at zero tables); no token/code unique index gains
`company_id`; every `share_tokens` read policy tests the company AND
`resolve_share_token` does not; the five service-role writers name a company.

Each was deliberately broken, run, watched fail, restored — six mutations, six
runs, each `1 failed`. One verbatim:

```
AssertionError: a read policy without a company test leaks resource_id across
carriers: expected [ 'Staff can view share tokens' ] to deeply equal []
```

## Suites

- `bunx vitest run src/test/tenancy-resolver.test.ts -t "B8 —"` — 6 passed, 108 skipped.
- policy-grant-parity, grant-parity-live, definer-search-path,
  caller-evaluated-functions, operator-settlement-isolation,
  notification-isolation, binder-share-authorization — 44 passed (7 files).
- `npx tsgo -p tsconfig.app.json --noEmit` — clean, after a deliberate
  `error TS2322` demonstration proved the command reports failure.

NOT claimed: a clean security linter. The migration reported the pre-existing
172 findings (5 types); none were remediated in this pass.

## Remaining scope, measured

48 tables / 9,630 rows lack `company_id`: the three global logs, `carrier_profile`
itself, `profiles`, the `applications` family and its PEI dependents, and the
settings/content tables already sorted. Nothing left is merely unstarted.

## Contradictions

1. The batch is 1,012 rows, not the re-cut's 1,004.
2. `application_resume_tokens` (56) was never in B8; it hangs off GLOBAL
   `applications` and waits on that decision.
3. Cross-carrier invisibility of the new `share_tokens` policy remains unproven
   with one carrier. The policy text, the fail-closed stamp and the anonymous
   path are proven; isolation is not.
